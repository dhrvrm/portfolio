---
import Layout from '../layouts/Layout.astro';
import ExperienceItem from '../components/ExperienceItem.astro';
import { getCollection } from 'astro:content';

const entries = (await getCollection('experience')).sort(
	(a, b) => a.data.order - b.data.order,
);
---

<Layout title='Experience - Dhruv Verma'>
	<div class='container mx-auto py-16'>
		<p class='text-sm uppercase tracking-[0.3em] text-muted'>Experience</p>
		<h1 class='text-3xl md:text-5xl font-semibold font-display mt-4 mb-12'>
			Building products with clarity and craft
		</h1>

		<section aria-label='Work history' class='experience-timeline'>
			<ol class='experience-timeline__list' start={1}>
				{
					entries.map((entry, i) => (
						<ExperienceItem
							itemId={`exp-${i}`}
							title={entry.data.title}
							company={entry.data.company}
							startDate={entry.data.startDate}
							endDate={entry.data.endDate}
							achievements={entry.data.achievements}
							isFirst={i === 0}
							isLast={i === entries.length - 1}
						/>
					))
				}
			</ol>
		</section>
	</div>
</Layout>

<style>
	.experience-timeline {
		max-width: 640px;
	}

	.experience-timeline__list {
		list-style: none;
		padding: 0;
		margin: 0;
	}
</style>
